<Page x:Class="GreenBeGoneUI.LandingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      Title="LandingPage"
      Background="{StaticResource BrushCharcoal}">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-50,0,0">
            <!-- Title Area -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,40">
                <TextBlock Text="ByeBye" Style="{StaticResource TitleSerif}" Foreground="{StaticResource BrushWhite}"/>
                <TextBlock Text="Green" Style="{StaticResource TitleSerif}" Foreground="{StaticResource BrushEmerald}" Margin="5,0,0,0"/>
                <!-- Emerald Starburst Icon -->
                <Viewbox Width="30" Height="30" Margin="12,8,0,0" VerticalAlignment="Top">
                    <Path Fill="{StaticResource BrushEmerald}" Data="M12,0 L14.5,9.5 L24,12 L14.5,14.5 L12,24 L9.5,14.5 L0,12 L9.5,9.5 Z"/>
                </Viewbox>
            </StackPanel>
            
            <!-- Interactive Slide-to-Start -->
            <Grid Width="400" Height="60">
                <Grid.Resources>
                    <Style x:Key="SliderThumbStyle" TargetType="Thumb">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Thumb">
                                    <Grid>
                                        <Border Width="50" Height="50" CornerRadius="25" Background="{StaticResource BrushEmerald}">
                                            <Path Data="M0,0 L10,6 L0,12 Z" Fill="{StaticResource BrushCharcoal}" Width="10" Height="12" Stretch="Fill" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Grid.Resources>

                <!-- Background Track -->
                <Border Background="{StaticResource BrushLighterCharcoal}" CornerRadius="30"/>
                
                <!-- Instruction Text -->
                <TextBlock Text="Slide to Start >>>" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center" 
                           Foreground="{StaticResource BrushEmerald}" 
                           FontFamily="Georgia"
                           FontSize="18"
                           IsHitTestVisible="False"/>

                <!-- The Slider -->
                <Slider Name="StartSlider" Minimum="0" Maximum="100" Value="0" VerticalAlignment="Center" Margin="5" ValueChanged="StartSlider_ValueChanged">
                    <Slider.Template>
                        <ControlTemplate TargetType="Slider">
                            <Grid>
                                <!-- Transparent Track to catch clicks/drags -->
                                <Track x:Name="PART_Track">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Opacity="0"/>
                                    </Track.DecreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb Style="{StaticResource SliderThumbStyle}"/>
                                    </Track.Thumb>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Opacity="0"/>
                                    </Track.IncreaseRepeatButton>
                                </Track>
                            </Grid>
                        </ControlTemplate>
                    </Slider.Template>
                </Slider>
            </Grid>
        </StackPanel>
        
        <!-- Footer -->
        <TextBlock Text="Made by Jakub Augustin" 
                   Style="{StaticResource BodySans}" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Bottom" 
                   Margin="0,0,0,30" 
                   Opacity="0.5"/>
    </Grid>
</Page>